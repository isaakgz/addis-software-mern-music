services:
  - type: web
    name: addismusc-backend
    env: docker
    plan: free
    dockerfilePath: Server/Dockerfile
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 4000
